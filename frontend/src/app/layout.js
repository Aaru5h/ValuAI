import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "ValuAI - Startup Valuation Platform",
  description: "Get AI-powered startup valuations instantly. Leverage machine learning to understand your company's worth based on revenue, industry, and team size.",
  keywords: ["startup valuation", "AI", "machine learning", "business valuation", "funding"],
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
